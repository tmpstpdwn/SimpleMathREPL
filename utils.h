// #DEFINES

#define STACKLEN 100 // length for stacks.
#define NUMLEN 13 // length for number storing buffer.
#define INBUFFLEN 1000 // length for input buffer.
#define PROMPT " $ " // prompt.
#define RESSYM " = " // symbol used before result.

// ENUMS

// enum to notify the lastread token type.
enum {NONE, OP, NUM};

// END
